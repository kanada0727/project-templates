version: "3.0"
services:
  jupyter-visualize:
    container_name: jupyter-visualize
    build:
      context: ..
      dockerfile: docker/Dockerfile
      target: prod
    environment:
      - SHELL=/bin/bash
    tty: true
